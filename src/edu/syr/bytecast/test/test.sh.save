#!/bin/bash
if [ "$#" -eq 2 ]; then
	./poc2 $1 $2
	exe_result=$?;

#	if [ "$exe_result" -eq "1" ]; then
#		echo "the letter in argument 1 has a smaller ascii value than the letter in argument 2"#
	else
		echo "the letter in argument 2 has a smaller ascii value than the letter in argument 1"

	fi
else
	echo "there were not the right number of arguments (two letters are needed)";
fi
